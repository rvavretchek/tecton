---
scope: Framework Tecton MVP completo - todas as 10 features do PRD, FR-1 a FR-31
purpose: build-substrate
altitude: initiative
updated: 2026-09-01T11:44
---

- (direction) Modo de trabalho: Coaching path (mesmo ritmo do PRD)
- (direction) Deliverables: spine de arquitetura + documentação UML em Markdown+Mermaid (componentes/domínios, sequência para sensitive.quorum/approval e extract/Strangler Fig, possivelmente classes/ER do Core de Diretório). Público: uso próprio do autor e futuros colaboradores, sem necessidade de material de apresentação polido pra revisor externo agora (SM-3 do PRD fica pra depois, se/quando precisar)
- (decision) Paradigma de design confirmado pelo autor: DOMA (Domain-Oriented Microservice Architecture) no nível macro (domínios como bounded contexts com fronteira/contrato explícitos) + Hexagonal/Ports-and-Adapters por serviço no nível micro (Providers do PRD são as portas: AuthProvider, KeyCustodyProvider, ServiceDiscoveryProvider, ConfigProvider, WorkflowEngineProvider, TokenRevocationStore). Não é decisão nova, é nomear formalmente o que o PRD já decidiu implicitamente
- (decision) Core de Diretório é um serviço próprio (opção 1): hospeda Tenant + Usuário/Grupo + Custodiante juntos num único 'serviço de diretório' do framework, com uma Closure Table fisicamente unificada. Isso é uma exceção explícita à regra geral de persistência-por-serviço do Glossário do PRD, justificada por serem domínios framework-provided (não escaneados por generate domain), inspirados em serviço de diretório centralizado real (NDS/Active Directory). Domínios de terceiros (Catálogo, Relatórios etc.) continuam cada um com seu próprio banco, sem exceção
- (version) TypeScript fixado em 6.0.3 (última release da implementação classica em JS) em vez de 7.0 (native Go compiler, GA em jul/2026) - TS 7 quebra ts-node e tsx (issue confirmada, API de compilador ainda instável), e tsx watch é mecanismo de live reload da FR-15. Revisitar quando o ecossistema tsx/testing acompanhar
- (version) Valkey adotado no lugar de Redis em toda a stack (Streams, rate limiting, TokenRevocationStore, Dev Services) - Redis Ltd mudou de BSD-3 para SSPL/RSALv2 em 2024, Valkey é o fork Linux Foundation BSD-3, clientes Node compatíveis sem mudança, sem gap de feature pro uso do Tecton (só primitivos puros). Mesmo raciocínio da troca Vault->OpenBAO já feita no PRD. Decisão propagada para PRD, brief, addendum do brief e docs/aether-tecton-compatibility.md (pendência de baixa relevância registrada pro lado do Aether)
- (version) Stack confirmado e verificado na web em 2026-08-28: Node.js 24 (Active LTS, suportado até abr/2028), Fastify 5.12.x, Prisma 7.8.x (100% TypeScript, sem engine Rust, stack trace mais legível - reforça objetivo 2 de ser IA-friendly)
- (decision) Directory Service (Tenant+Usuário/Grupo+Custodiante) nasce como serviço pronto/configurável, distribuído como pacote versionado do próprio Tecton (@tecton/directory ou equivalente) - NUNCA copiado/scaffolded pro repo do dev, ao contrário de domínios de negócio via generate domain. Customização só via objectClass.attributes declarativo no manifest, nunca editando lógica interna. Mesma postura 'nunca reimplementar, sempre integrar' já aplicada ao Custodiante (Constitution §7). Define regra de dependência: pacotes @tecton/* são importados como dependência versionada, nunca copiados pro código gerado; nenhum domínio de negócio importa código interno de outro domínio ou do Directory Service
- (decision) Terceiro eixo de idioma confirmado (distinto de docs-PT-BR vs código-inglês): mensagens/superfícies expostas ao usuário final do sistema GERADO pelo Tecton (corpo de erro RFC 9457 title/detail, UI React gerada via react-jsonschema-form, telas do Directory Service) são multi-idioma - PT-BR principal, EN secundário, extensível. CLI (tecton-admin) fica de fora, só inglês (público é dev/agente de IA). Mecanismo: RFC 9457 type continua URI neutra de idioma; title/detail negociados por Accept-Language; campo de extensão i18nKey pra lookup de máquina. Framework ship PT-BR+EN pras próprias superfícies (Directory Service, boilerplate de erro); fornece infraestrutura de i18n (catálogo de mensagens) pra domínios do dev, sem obrigar tradução do código de terceiros - é possível, não obrigatório
- (decision) Estrutura de pacotes do monorepo do framework (pnpm workspaces, sem Turborepo/Nx nesse repo) confirmada: @tecton/manifest (schema/parser/validador, sem dependências internas), @tecton/core (geração de rota Fastify, conector Valkey Streams/CloudEvents, formato de resposta FR-23/24/25 com i18n, health checks), @tecton/providers (interfaces de Provider + implementações de referência), @tecton/directory (Directory Service pronto), @tecton/service-client (gerador do ServiceClient), @tecton/cli (tecton-admin, depende de todos). Regra de dependência (vira AD da spine): manifest não depende de nada interno; core/providers/directory/service-client podem depender de manifest; cli depende de todos; nunca o inverso. Código gerado importa @tecton/* como dependência versionada, nunca copia
- (decision) UUID v7 confirmado como formato canônico de ID para toda entidade/evento no Tecton (Core de Diretório, domínios, campo id do CloudEvents). Ordenável por tempo de criação (bom pra índice de Closure Table), suportado nativamente por Prisma nos três bancos alvo, evita fragmentação de índice B-tree que UUID v4 causa
- (event) Primeiro rascunho completo da ARCHITECTURE-SPINE.md destilado do memlog: 7 ADs (paradigma DOMA+Hexagonal, Core de Diretório como serviço próprio, direção de dependência entre pacotes do framework, código gerado importa/nunca copia, UUID v7, i18n como terceiro eixo, Zero Trust em toda borda), Consistency Conventions, Stack verificado, Structural Seed com 3 diagramas mermaid (hexagonal, dependência de pacotes, sistema/container), Capability Map, e 6 itens Deferred. Ainda em draft, pendente revisão do autor antes do Reviewer Gate
- (event) Reviewer Gate rodado (rubric walker + verificação de versão + adversarial). Fixes claros aplicados: AD-7 fechada contra confused-deputy (decisão de autorização só com claim auto-verificada); AD-5 encoding de UUID v7 fixado; AD-8 nova (Gateway fino com lint:gateway); AD-9 nova (isolamento de domínio - nunca DB nem código de outro domínio, único caminho é ServiceClient ou events); AD-2 ganhou regra de acesso ao Directory via eventos, não leitura direta de banco; Capability Map corrigido (4.6 e 4.8 apontavam pra AD errada); Prisma bump 7.8→8.x (GA 3 dias atrás); Valkey pin 9.1.x; OpenTelemetry e Awilix/DI-IoC adicionados ao Stack (estavam ausentes apesar de MVP In Scope no PRD §6.1). Deixado em aberto pro autor: mecanismo de armazenamento de objectClass.attributes (coluna relacional vs JSON/JSONB) - achado adversarial real, bloqueia um caso de uso central se não resolvido
- (decision) Mecanismo de armazenamento de objectClass.attributes confirmado: bag de atributos JSON/JSONB por objeto no Directory Service, validado contra o JSON Schema do objectClass em tempo de escrita - nunca migração de schema relacional disparada por manifest de terceiros. Mesmo padrão LDAP/Active Directory. Trade-off aceito: sem unicidade/FK/index garantidos pelo banco nesses atributos, só validação de aplicação; atributo que precisa de constraint forte pertence a domínio de negócio próprio, não ao Directory Service. Fecha o gap H9 achado pelo revisor adversarial
- (change) Reconciliação manual contra o PRD (agente de reconciliação bateu em rate-limit, refeito inline): Testcontainers (FR-31, MVP In Scope §6.1) estava ausente do Stack/Structural Seed da spine apesar de já estar decidido no PRD - adicionado ao Stack. Demais capacidades de §6.1 confirmadas com home na spine (FR-11 interceptação de dado, FR-20 ServiceDiscoveryProvider trocável, domínios embutidos, versionamento §8 sem conflito com AD-3/AD-4)
- (change) Reconciliação com Constitution (subagente): confirmado limpo em §7 (crypto vendor-agnostic), §1 (Aether sem menção fora do ponteiro sancionado). Corrigidos 2 gaps menores: convenção de naming agora declara identificadores em inglês explicitamente (eixo 2, §8), não só implícito; diagrama de sistema (spine + UML.md) anotado com AD-7 em toda seta Gateway->serviço e chamada entre domínios, não só Gateway->AuthSvc, deixando claro que a validação do gateway é primeira linha, nunca a única (FR-13)
- (event) UML.md criado (companion da spine): componentes (pacotes do framework, sistema em runtime, anatomia hexagonal), sequência (sensitive.quorum/approval, extract/Strangler Fig, evento CloudEvents/Valkey Streams), e ER do Core de Diretório (DIRECTORY_OBJECT unificado com objectClass+attributes JSON, CLOSURE_EDGE, ACL_GRANT)
